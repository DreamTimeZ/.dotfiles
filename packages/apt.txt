# System prerequisites for Ubuntu/Debian (WSL and Linux)
# These are needed before Homebrew or as system-level dependencies.

build-essential
curl
git
file
procps
# Build dependencies for cargo crates (OpenSSL, pkg-config)
pkg-config
libssl-dev
zsh
tmux
